<Window x:Class="LlamaCppLoader.SaveProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Profile" Height="180" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Profile Name:" Margin="0,0,0,8" FontSize="13"/>
        <TextBox x:Name="ProfileNameTextBox" Grid.Row="1" Padding="8" FontSize="13" Height="32"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Width="90" Height="32" IsDefault="True" Click="Save_Click" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="Black" FontWeight="Bold" FontSize="13"/>
            <Button Content="Cancel" Width="90" Height="32" IsCancel="True" Click="Cancel_Click" FontSize="13"/>
        </StackPanel>
    </Grid>
</Window>
