# 项目目录结构

```
llamacpploader/
│
├── 核心代码文件
│   ├── LlamaCppLoader.csproj          # 项目配置文件 (.NET 8.0)
│   ├── App.xaml                        # 应用程序资源定义
│   ├── App.xaml.cs                     # 应用程序入口代码
│   ├── MainWindow.xaml                 # 主窗口界面定义 (8.5 KB)
│   ├── MainWindow.xaml.cs              # 主窗口逻辑代码 (16 KB, 470+ 行)
│   ├── SaveProfileDialog.xaml          # 配置保存对话框界面
│   └── SaveProfileDialog.xaml.cs       # 配置保存对话框逻辑
│
├── 文档文件
│   ├── README.md                       # 项目主文档 (功能介绍、安装说明)
│   ├── QUICKSTART.md                   # 快速入门指南 (详细步骤)
│   ├── HELP.md                         # 使用帮助 (参数详解、FAQ)
│   ├── PROJECT_OVERVIEW.md             # 技术概览 (架构、实现细节)
│   └── 项目完成说明.md                  # 项目完成总结
│
├── 工具文件
│   ├── build.bat                       # Windows 构建脚本
│   ├── publish.bat                     # Windows 发布脚本
│   └── profiles.example.json           # 配置文件示例
│
└── 配置文件
    └── .gitignore                      # Git 忽略规则

构建后生成（不包含在源代码中）:
├── bin/
│   ├── Debug/
│   │   └── net8.0-windows/
│   │       └── LlamaCppLoader.exe      # Debug 版本
│   └── Release/
│       └── net8.0-windows/
│           ├── LlamaCppLoader.exe      # Release 版本
│           └── win-x64/
│               └── publish/
│                   └── LlamaCppLoader.exe  # 独立发布版本 (~100-150MB)
└── obj/                                # 临时编译文件
```

## 文件说明

### 必需文件（运行时）
这些文件是程序运行所必需的：
- `LlamaCppLoader.csproj`
- `App.xaml` / `App.xaml.cs`
- `MainWindow.xaml` / `MainWindow.xaml.cs`
- `SaveProfileDialog.xaml` / `SaveProfileDialog.xaml.cs`

### 可选文件（开发/文档）
这些文件用于开发和用户参考：
- 所有 `.md` 文件（文档）
- `build.bat` / `publish.bat`（构建工具）
- `profiles.example.json`（示例）
- `.gitignore`（版本控制）

### 运行时生成的文件
程序运行时会在用户目录创建：
```
C:\Users\<用户名>\AppData\Roaming\LlamaCppLoader\
└── profiles.json                       # 用户保存的配置文件
```

## 代码统计

| 文件类型 | 文件数 | 代码行数 | 大小 |
|---------|-------|---------|------|
| C# 代码 | 3 | ~500 行 | 17 KB |
| XAML 界面 | 3 | ~250 行 | 11 KB |
| 文档 | 5 | ~1000 行 | 35 KB |
| 脚本/配置 | 4 | ~100 行 | 5 KB |
| **总计** | **15** | **~1850 行** | **68 KB** |

## 构建产物

### Debug 构建
- 位置: `bin\Debug\net8.0-windows\`
- 大小: ~100 KB (不含 .NET Runtime)
- 用途: 开发调试

### Release 构建
- 位置: `bin\Release\net8.0-windows\`
- 大小: ~100 KB (不含 .NET Runtime)
- 用途: 需要 .NET 8.0 Runtime 的部署

### Self-Contained 发布
- 位置: `bin\Release\net8.0-windows\win-x64\publish\`
- 大小: ~100-150 MB (包含所有依赖)
- 用途: 独立分发，无需安装 .NET Runtime

## 快速开始

### 1. 构建项目
```bash
# 使用脚本
build.bat

# 或手动构建
dotnet restore
dotnet build -c Release
```

### 2. 发布独立应用
```bash
# 使用脚本
publish.bat

# 或手动发布
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

### 3. 运行程序
双击运行生成的 `LlamaCppLoader.exe`

## 文档阅读顺序

1. **README.md** - 了解项目功能和基本使用
2. **QUICKSTART.md** - 学习如何构建和首次配置
3. **HELP.md** - 详细了解参数和高级功能
4. **PROJECT_OVERVIEW.md** - 深入理解技术实现（开发者）
5. **项目完成说明.md** - 项目总结和特色功能

## 许可证

MIT License - 可自由使用、修改和分发

## 联系方式

- 提交 Issue 报告问题
- 提交 Pull Request 贡献代码
- 查看文档获取更多帮助
