# 🆕 新功能：版本检测和更新提示

## 功能概述

v1.1.0 版本新增了自动版本检测功能，可以：
1. ✅ 自动检测本地 llama-server.exe 的版本
2. ✅ 从 GitHub 获取 llama.cpp 的最新版本
3. ✅ 对比版本并提示升级
4. ✅ 一键跳转到 GitHub Release 页面下载

---

## 界面变化

### 版本信息栏

程序顶部新增了一个蓝色的版本信息栏：

```
┌─────────────────────────────────────────────────────────┐
│ 本地: build 1234 (a1b2c3d) | 最新: b4567    🔔 发现新版本！ │ 🔄 刷新版本
└─────────────────────────────────────────────────────────┘
```

**显示信息**：
- **本地版本**：当前 llama-server.exe 的版本号
- **最新版本**：GitHub 上的最新 release 版本
- **更新提示**：如果有新版本，显示蓝色通知链接
- **刷新按钮**：手动刷新版本信息

---

## 使用方法

### 自动检测

程序启动时会自动：
1. 检测 llama-server.exe 的版本（如果已选择路径）
2. 连接 GitHub API 获取最新版本
3. 显示在顶部版本信息栏

**示例输出**：
```
本地: build 1234 (a1b2c3d)  |  最新: b4567
```

### 选择服务器后自动检测

当你通过 "Browse..." 选择 llama-server.exe 后，程序会自动检测该文件的版本。

### 手动刷新

点击右上角的 **🔄 刷新版本** 按钮，重新检测版本信息。

使用场景：
- 更新了 llama-server.exe 文件
- 网络连接恢复
- 想查看是否有新版本发布

### 查看更新

如果发现新版本，会显示：
```
🔔 发现新版本！点击查看
```

**点击链接后**：
- 自动打开浏览器
- 跳转到 llama.cpp 的 GitHub Release 页面
- 可以下载最新版本

---

## 版本信息示例

### 情况 1：版本最新
```
本地: build 4567 (a1b2c3d)  |  最新: b4567
```
无更新提示，表示已是最新版。

### 情况 2：有新版本
```
本地: build 4000 (a1b2c3d)  |  最新: b4567  🔔 发现新版本！点击查看
```
显示蓝色通知，点击可查看更新。

### 情况 3：未选择服务器
```
未选择服务器  |  最新: b4567
```
选择 llama-server.exe 后会自动检测本地版本。

### 情况 4：网络错误
```
本地: build 4567 (a1b2c3d)  |  网络错误: ...
```
无法连接 GitHub，但本地版本仍然显示。

---

## 技术细节

### 本地版本检测

程序通过运行以下命令获取版本：
```bash
llama-server.exe --version
```

**支持的版本格式**：
- `version: 1234 (a1b2c3d)` - 标准格式
- `build: 1234` - 构建号格式
- 文件版本信息（FileVersionInfo）

### 远程版本获取

通过 GitHub API 获取：
```
https://api.github.com/repos/ggerganov/llama.cpp/releases/latest
```

**返回信息**：
- 最新 release 的标签名（如 `b4567`）
- Release 页面 URL

### 版本比较

程序会比较：
1. 构建号（build number）
2. 标签名（tag name）

只有当确认有更新时，才显示通知。

---

## 常见问题

### Q: 显示"检测版本中..."一直不变
**A**: 可能原因：
- llama-server.exe 路径未设置
- 网络连接问题
- GitHub API 访问受限

**解决方法**：
1. 确认已选择 llama-server.exe
2. 检查网络连接
3. 点击"刷新版本"重试

### Q: 版本号显示为"未知版本"
**A**: 可能原因：
- llama-server.exe 版本太旧
- 文件损坏
- 不是标准的 llama.cpp 构建

**解决方法**：
- 下载最新的官方版本
- 确认文件完整性

### Q: 无法获取最新版本
**A**: 可能原因：
- 网络连接问题
- GitHub API 限流
- 防火墙阻止

**解决方法**：
- 检查网络连接
- 稍后重试
- 手动访问 https://github.com/ggml-org/llama.cpp/releases

### Q: 点击更新链接没反应
**A**: 可能原因：
- 浏览器未设置
- 系统权限问题

**解决方法**：
- 手动复制链接到浏览器
- 直接访问 https://github.com/ggml-org/llama.cpp/releases/latest

### Q: 如何更新 llama.cpp？
**A**: 步骤：
1. 点击"发现新版本"链接
2. 在 GitHub Release 页面下载对应版本
3. 解压并替换旧的 llama-server.exe
4. 在程序中重新选择路径或点击刷新

---

## 配置说明

### 网络超时

默认超时时间：**10 秒**

如果网络较慢，版本检测可能失败。这是正常的，不影响程序主要功能。

### 自动检查时机

程序会在以下情况自动检查版本：
1. **程序启动时**
2. **选择新的服务器路径时**

### 手动刷新

任何时候都可以点击 **🔄 刷新版本** 按钮手动触发检查。

---

## 隐私说明

版本检测功能：
- ✅ 仅读取本地 llama-server.exe 的版本信息
- ✅ 通过 GitHub 公开 API 获取最新版本
- ✅ 不收集或上传任何个人信息
- ✅ 不追踪使用行为
- ✅ 所有操作本地完成

**网络请求**：
- 目标：`api.github.com`
- 频率：仅在启动、选择文件或手动刷新时
- 内容：获取最新 release 信息
- User-Agent: `LlamaCppLoader/1.0`

---

## 更新步骤示例

### 完整更新流程

1. **发现更新**
   ```
   本地: build 4000  |  最新: b4567  🔔 发现新版本！
   ```

2. **点击链接**
   - 打开 GitHub Release 页面

3. **下载新版本**
   - 选择对应平台的文件（如 `llama-b4567-win-avx2.zip`）
   - 下载到本地

4. **替换文件**
   ```bash
   # 备份旧版本
   copy llama-server.exe llama-server.exe.bak
   
   # 解压新版本并替换
   # 将新的 llama-server.exe 复制到原位置
   ```

5. **验证更新**
   - 在程序中点击 **🔄 刷新版本**
   - 确认版本号已更新

---

## 界面截图说明

### 版本信息栏位置
```
┌─────────────────────────────────────────────────┐
│ Llama.cpp Server Loader                    _ □ ×│
├─────────────────────────────────────────────────┤
│ 本地: build 4567 | 最新: b4567    [🔄 刷新版本] │ ← 版本信息栏
├─────────────────────────────────────────────────┤
│ ╔════════════════════════════════════════════╗  │
│ ║ Server Configuration                       ║  │
│ ║  llama-server.exe Path: [____] [Browse...] ║  │
│ ...                                              │
```

### 有更新时
```
┌─────────────────────────────────────────────────┐
│ 本地: build 4000 | 最新: b4567                   │
│ 🔔 发现新版本！点击查看         [🔄 刷新版本]    │ ← 蓝色通知
└─────────────────────────────────────────────────┘
```

---

## 下一步

### 待关闭程序后重新构建

**当前状态**：代码已更新，等待重新构建

**操作步骤**：
1. **关闭正在运行的程序**
2. **重新构建**：
   ```bash
   dotnet build -c Release
   ```
   或双击 `build.bat`
3. **启动程序查看新功能**

---

## 功能优势

✅ **自动化** - 无需手动检查更新
✅ **便捷** - 一键跳转下载页面
✅ **及时** - 第一时间知道新版本
✅ **安全** - 通过官方 GitHub API
✅ **轻量** - 不影响主要功能
✅ **智能** - 只在有更新时提示

---

**享受新功能！** 🎉
